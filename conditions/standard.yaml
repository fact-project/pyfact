name: standard
description: Standard condition set used by erna
conditions: [
    fRunTypeName = "data"
    fZenithDistanceMean < 30,
    fTriggerRateMedian > 40,
    fTriggerRateMedian < 85,
    fEffectiveOn > 0.95,
]